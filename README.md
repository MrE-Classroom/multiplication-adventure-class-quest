# Multiplication Adventure Class Quest v16 robust fix

Upload these files over the current GitHub Pages game files. Keep the existing assets folder.

Fixes: robust shop rendering, migration for older saved states, safer item filtering, and render recovery instead of a blank screen.
